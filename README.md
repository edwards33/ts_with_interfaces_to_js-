# TypeScript with Interfaces to JS dist

  Install TypeScript:

```bash
$ npm i -g typescript
```
  Install Http-server

```bash
$ npm i -g http-server
```

  Compile to JS:

```bash
$ tsc
```

  Run Server:

```bash
$ http-server -c-1
```
